//Return true if the given string is a palindrome.
//Otherwise, return false.

//lowercase - remove symbols and space (join)
//if str é par, slice 0 -> length/2 == length/2 + 1
// 

function palindrome(str) {
    str.toLowerCase()

}
  
palindrome("eye");

palindrome("_eye");

palindrome("not a palindrome") 
