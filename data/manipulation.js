//verificar se contém palavra Amor

let phrase = "Eu quero viver!"
console.log(phrase.includes("Amor"))

//returns false -do not contain the word
//case sensitive
//return boolean

//criar Array com construtor

let myArray = new Array('a','b','c')
console.log(myArray)

//to count: .length

let word = "manipulation"
console.log(Array.from(word))
//transform string into a array elements

/*.
push = add a item at the end
.unshift = add item at the beginner
.pop = remove the last item
.shift = remove the first item
.slice(firstelement,untilthiselement)- take the element from array
.splice(index,howManyElementsAfter) =remove elements from array
*/

let index = myArray.indexOf('b')
console.log(index)
//find the index of an element in the array

